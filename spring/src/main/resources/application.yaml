spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clinica_veterinaria
    username: postgres
    password: TU_PASSWORD_AQUI
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

api:
  token: Bearer TU_TOKEN_RENIEC_AQUI